export const injectExtraPropsKey: unique symbol;
export default FunctionProvider;
declare namespace FunctionProvider {
    const inheritAttrs: boolean;
    const isMenuProvider: boolean;
    namespace props {
        const extraProps: any;
    }
    function setup(props: any, { slots }: {
        slots: any;
    }): () => any;
    function setup(props: any, { slots }: {
        slots: any;
    }): () => any;
}
