import { createVNode as _createVNode } from "vue";
import { createNamespace } from '../utils';
import { useChildren } from '@vant/use';
var [createComponent, bem] = createNamespace('steps');
export var STEPS_KEY = 'vanSteps';
export default createComponent({
  props: {
    finishIcon: String,
    activeColor: String,
    inactiveIcon: String,
    inactiveColor: String,
    active: {
      type: [Number, String],
      default: 0
    },
    direction: {
      type: String,
      default: 'horizontal'
    },
    activeIcon: {
      type: String,
      default: 'checked'
    }
  },
  emits: ['click-step'],

  setup(props, {
    emit,
    slots
  }) {
    var {
      linkChildren
    } = useChildren(STEPS_KEY);

    var onClickStep = index => {
      emit('click-step', index);
    };

    linkChildren({
      props,
      onClickStep
    });
    return () => _createVNode("div", {
      "class": bem([props.direction])
    }, [_createVNode("div", {
      "class": bem('items')
    }, [slots.default == null ? void 0 : slots.default()])]);
  }

});